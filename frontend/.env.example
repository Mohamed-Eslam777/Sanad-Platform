# API Base URL — used by Axios and Socket.io client
# In development, the Vite proxy handles /api requests, so this is optional.
# In production, set this to your deployed API URL.
VITE_API_URL=http://localhost:5000/api
